<!-- ⛔ 开发者：自由的风 · 永久钢印 · 禁止删除 -->
# ⛔ 统一调度层 · 唯一执行通道 · 不可跳步

> **这是GBT大脑的唯一执行路径。没有第二条路。**
> **每次收到任务必须按 Step -1→0→1→2→3→4→5→6 执行。**
> **每步必须显式输出 `✅ Step N 完成: [证据]` 才能进入下一步。**
> **Step -1(邻域感知) 在每次任务启动时自动执行——快速自排查所有邻域状态。**
---

## 通道地图

```
┌─────────────────────────────────────────────────────────┐
│ STEP -1 — 邻域感知（任务启动前自动执行）                  │
│ → 全邻域扫描: 检查38个邻域/166能力模块状态                  │
│ → 快速诊断: 发现缺失/异常立即预警                          │
│ → 能力路由预检: 确认所需能力可用                          │
│ → 输出: "🔍 Step -1: 邻域=[健康%], 缺陷=[列表]"           │
│ ▸ 门禁: 核心模块全部就绪? 有缺陷已找到替代?               │
└──────────────────────┬──────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│ STEP 0 — 任务登记                                       │
│ → 从用户指令提取任务描述（≤3句）                         │
│ → 输出: "📥 Step 0: 任务=[描述], 优先级=[高/中/低]"      │
│ ▸ 门禁: 复述正确? 用户确认?                              │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 1 — 影响面分析                                     │
│ → 列出涉及文件/系统/能力（用glob/grep确认，不可猜）       │
│ → 输出: "📋 Step 1: 涉及=[列表]"                         │
│ ▸ 门禁: 每个涉及项都有工具证据?                          │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 2 — 读取研究（每个涉及的文件必须read）               │
│ → 用read读取相关文件（不是grep一行）                      │
│ → 用glob探索不明确的结构                                  │
│ → 输出: "📖 Step 2: 已读=[文件:行范围]..."                │
│ ▸ 门禁: 每个涉及文件都读过了?                             │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 3 — 方案设计                                       │
│ → 列出每个要修改的文件+内容+连锁影响+验收标准              │
│ → 对比至少2种方案，说明选择原因                           │
│ → 输出: "📝 Step 3: 方案=[描述], 子任务=[列表]"           │
│ ▸ 门禁: 方案有验收标准? 考虑了连锁影响?                    │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 4 — 逐子任务执行（每个子任务走子通道）               │
│                                                         │
│  对每个子任务(文件):                                     │
│  ┌──────────────────────────────────────────────┐       │
│  │ 4a. re-read 确认文件最新内容                   │       │
│  │ 4b. 执行修改                                   │       │
│  │ 4c. 立即验证（编译/语法/逻辑）                  │       │
│  │ 4d. 输出: "✅ 子任务: [文件] 完成, 验证=[结果]" │       │
│  │ 4e. 失败 → 分析根因 → 修正 → 重新4c           │       │
│  └──────────────────────────────────────────────┘       │
│                                                         │
│  所有子任务完成 → 输出: "🔧 Step 4: 全部子任务完成"       │
│ ▸ 门禁: 每个子任务都通过4a→4d?                           │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 5 — 深度复查（必须三遍，查出零问题）                  │
│                                                         │
│  第1遍: 实际运行验证                                     │
│  → 启动/运行 → 触发改动路径 → 观察结果                   │
│  → 输出: "🔍 Step 5-1: 运行验证=[通过/失败]"             │
│                                                         │
│  第2遍: 逐diff检查                                       │
│  → 读所有改动文件的diff → 逐行检查逻辑                    │
│  → 输出: "🔍 Step 5-2: diff检查=[通过/失败]"             │
│                                                         │
│  第3遍: 回归+边界验证                                    │
│  → 检查连锁影响: 所有调用方/依赖方是否正常                │
│  → 边界测试: 空输入/超长/特殊字符/并发                    │
│  → 输出: "🔍 Step 5-3: 回归检查=[通过/失败]"             │
│                                                         │
│  任一失败 → 回Step 2分析根因 → 修完重新三遍               │
│  三遍都通过 → 输出: "✅ Step 5: 三遍复查通过"              │
│ ▸ 门禁: 三遍都通过? 查出零问题? 回归无影响?               │
```


## 🚫 禁止路径（任何跳过 = 死机）

```
禁止: 用户指令 → 直接调工具          → 必须: 用户指令 → Step 0 登记
禁止: Step 0 → Step 4 (跳过研究)      → 必须: Step 0 → 1 → 2 → 3 → 4
禁止: Step 4 → Step 6 (跳过复查)      → 必须: Step 4 → 5(三遍) → 6
## 子通道规则（Step 4内每个文件/操作·四脑全程关注）

每次编辑/修改一个文件前，必须嵌入四脑标记：

```
🔧 子任务: [文件名]
   🧠推理脑: [为什么必须改这个文件? 逻辑依据?]
   💻编程脑: [具体改什么? 怎么改? 有几种方案?]
   4a-read: [已读最新内容? #TAG]
   4b-修改: [改什么? 为什么必须改?]
   4c-验证: [改完后怎么验证?]
   4d-结果: [验证通过? 推理脑确认?]
```

子任务未完成🧠→💻→4a→4b→4c→4d全部步骤 → 不可开始下一个子任务。
**无🧠推理脑标记的操作 = 无脑操作 = 禁止。**
---

## 📊 进度追踪

每次回复必须附带进度条：

```
═══════════════════════════════════════════════
  任务: [名称]
  通道: Step 0□ 1□ 2□ 3□ 4□ 5□ 6□
  子任务: [已完成]/[总数]  当前: [文件名]
  跳步检测: [无 / 发现跳步→已回退Step N]
═══════════════════════════════════════════════
```
